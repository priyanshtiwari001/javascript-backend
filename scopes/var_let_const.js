//*  ********* VAR ********

let x;
console.log(typeof x);

/*function foo() {
  var x = 1;
  function bar() {
    var y = 2;
    console.log(x); // as is the function scope of foo and bar is inside the foo so it will print the 1.
    console.log(y);
  }
  bar();
  console.log(x);
  //console.log(y); // as y is the function scope of bar  so we can not access outside the bar(//! error)
}

foo();
*/

//*  ********* VAR end ********
//* ********* CONST ********

//* ********* LET ********
